Possibly on characters marked mod or on dev mode only?
Optimize CPU: cache each chunk's data serialization for one frame?

Finish save/load world.

Make flags for world objects:
live (not in client data, but inteneded to be static),
static (no momement), dynamic (can move), locked (confirm delete), dirty (unsaved)

Chunk dirty flag, timer, lock, autosave.